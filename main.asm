# Дизассемблировано из: cd f0 d0 09 c0 d0 80 c0 d9 d3 10...

asect 0x00
    ldi r3, 0xF0    # cd f0 — Загрузка константы в r3
    st r0, r1       # d0 09 — Ошибка интерпретации (вероятно, смещение)
    ldi r0, 0xD0    # c0 d0 — Загрузка 0xD0 в r0[cite: 2]
    halt            # 80 — Остановка процессора[cite: 2]
    
    # Далее идут команды, которые выглядят как обработчик или данные:
    ldi r0, 0xD9    # c0 d9[cite: 2]
    st r3, r0       # d3 10 (со смещением 0x10)[cite: 2]
    st r0, r2       # d0 f2[cite: 2]
    wait            # 3a — Процессор засыпает до прерывания[cite: 2]
    shl r2          # 0a — Сдвиг влево[cite: 2]
    bnz 0x14        # e1 14 — Переход, если не ноль[cite: 2]
    rti             # 2f — Возврат из прерывания[cite: 2]
    beq 0x15        # ee 15 — Переход, если ноль[cite: 2]
    push r3         # 93 — Сохранение r3[cite: 2]
    bc 0x19         # e3 19 — Переход по переносу[cite: 2]
    st r3, r0       # d3 10[cite: 2]
    pop r3          # a3 — Восстановление r3[cite: 2]
    beq 0x0e        # ee 0e[cite: 2]
    js r2           # 82 — Перейти по адресу в r2[cite: 2]